/*
  CONFIGURAÇÃO DO PROJETO (lado do navegador)
  ---------------------------------------------
  Nada de chaves ou credenciais aqui — tudo que precisa da service account do
  Google fica em variáveis de ambiente no servidor (veja README.md, seção
  "Configurar o backend"). Este arquivo só existe para futuras opções de UI.
*/
window.APP_CONFIG = {};
